version: '3.8'
services:
  backend:
    build: .
    ports:
      - "4000:4000"
    environment:
      - RAPIDAPI_KEY
      - RAPIDAPI_HOST
      - CACHE_TTL_SECONDS=60
    depends_on:
      - redis
  redis:
    image: redis:7-alpine
    restart: unless-stopped
    ports:
      - "6379:6379"
