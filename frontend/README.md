# Rock Stock — Frontend

This is an Angular 16+ frontend scaffold that calls the backend API at `/api`.

Setup

```bash
cd frontend
npm install
ng serve
```

Environment: edit `src/environments/environment.ts` to set `apiBaseUrl`.
